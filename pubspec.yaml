name: text_gradient_widget
description: >-
  A Flutter widget for rendering text with linear and radial gradient effects,
  supporting 8 directions, custom stops, and full text styling.
version: 1.0.0
homepage: https://github.com/AmmarTN/text_gradient_widget
repository: https://github.com/AmmarTN/text_gradient_widget
issue_tracker: https://github.com/AmmarTN/text_gradient_widget/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

topics:
  - gradient
  - text
  - widget
  - ui
  - custom-widget

flutter:
